Olá, {{ user.get_full_name|default:user.get_username }}.

Recebemos uma solicitação para redefinir a senha da sua conta no SIGEE.

Acesse o link abaixo para escolher uma nova senha. O link é individual e expira em {{ password_reset_timeout_minutes }} minutos:

{{ protocol }}://{{ domain }}{% url 'password_reset_confirm' uidb64=uid token=token %}

Se você não fez esta solicitação, ignore esta mensagem. Sua senha atual continuará válida.

Equipe SIGEE
